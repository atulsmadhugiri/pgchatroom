import assert from 'assert';
import expect from 'expect';

import convertToCSV from '../js/json_to_csv';

describe('json_to_csv.js', () => {
  describe('convertToCSV', () => {
    expect(convertToCSV).toExist();
  });
});
